import { NavLink } from "react-router-dom";

const Navbar = () => {
  return (
    <div>
      <NavLink to="/">home</NavLink>
      <NavLink to="/">home</NavLink>
      <NavLink to="/">home</NavLink>
    </div>
  );
};

export default Navbar;
